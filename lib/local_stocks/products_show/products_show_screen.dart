import 'package:flutter/material.dart';

class ProductsShowScreen extends StatelessWidget {
  const ProductsShowScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ProductsShowScreen"),
      ),
    );
  }
}
